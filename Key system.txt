-- Source code
-- Edit this script by changing the code 
-- If you see any comment marks "-- " that means you can edit
-- Tutorial Script Are Here: loadstring(game:HttpGet("https://gist.githubusercontent.com/Angelo-Gitland/c1732ecf40925660d4dceb9cdc0b83e5/raw/bd572b3f9e45427d09ad9b907bf6f5446f614ad3/Key%2520System%2520Gui%2520Tutorials"))()

local player = game.Players.LocalPlayer
local gui = Instance.new("ScreenGui")
gui.Name = "MYKEYSYSTEMGUI"
gui.Parent = player:WaitForChild("PlayerGui")
gui.ResetOnSpawn = false

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 450, 0, 280)
mainFrame.Position = UDim2.new(0.5, -225, 0.5, -140)
mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
mainFrame.BackgroundTransparency = 0.2
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Parent = gui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 15)
corner.Parent = mainFrame

local gradient = Instance.new("UIGradient")
gradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(40, 40, 40)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(10, 10, 10))
})
gradient.Rotation = 90
gradient.Parent = mainFrame

local shadow = Instance.new("ImageLabel")
shadow.Size = UDim2.new(1, 30, 1, 30)
shadow.Position = UDim2.new(0, -15, 0, -15)
shadow.BackgroundTransparency = 1
shadow.Image = "rbxassetid://1316045217" -- Changeable
shadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
shadow.ImageTransparency = 0.5
shadow.ScaleType = Enum.ScaleType.Slice
shadow.SliceCenter = Rect.new(10,10,118,118)
shadow.Parent = mainFrame

local title = Instance.new("TextLabel")
title.Size = UDim2.new(0, 420, 0, 40)
title.Position = UDim2.new(0, 15, 0, 15)
title.Text = "My Key System" -- Changeable
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.BackgroundTransparency = 1
title.TextXAlignment = Enum.TextXAlignment.Left
title.Font = Enum.Font.GothamBold
title.TextSize = 28
title.Parent = mainFrame

local subtitle = Instance.new("TextLabel")
subtitle.Size = UDim2.new(0, 420, 0, 30)
subtitle.Position = UDim2.new(0, 15, 0, 55)
subtitle.Text = "Developed By TA7_GHOST" -- Changeable
subtitle.TextColor3 = Color3.fromRGB(200, 200, 200)
subtitle.BackgroundTransparency = 1
subtitle.TextXAlignment = Enum.TextXAlignment.Left
subtitle.Font = Enum.Font.Gotham
subtitle.TextSize = 20
subtitle.Parent = mainFrame

local textbox = Instance.new("TextBox")
textbox.Size = UDim2.new(0, 300, 0, 45)
textbox.Position = UDim2.new(0.5, -150, 0, 100)
textbox.PlaceholderText = "Text Here!" -- Changeable
textbox.Text = "" -- Changeable
textbox.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
textbox.TextColor3 = Color3.fromRGB(255, 255, 255)
textbox.Font = Enum.Font.GothamSemibold
textbox.TextSize = 20
textbox.Parent = mainFrame

local tbCorner = Instance.new("UICorner")
tbCorner.CornerRadius = UDim.new(0, 10)
tbCorner.Parent = textbox

local function makeButton(name, text, posX, posY)
    local btn = Instance.new("TextButton")
    btn.Name = name
    btn.Size = UDim2.new(0, 110, 0, 40)
    btn.Position = UDim2.new(0, posX, 0, posY)
    btn.Text = text -- Changeable
    btn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 18
    btn.Parent = mainFrame
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = btn
    btn.MouseEnter:Connect(function()
        btn.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
    end)
    btn.MouseLeave:Connect(function()
        btn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    end)
    return btn
end

local checkKey = makeButton("CheckKey", "Check Key", 20, 170) -- Changeable
local getKey = makeButton("GetKey", "Get Key", 170, 170) -- Changeable
local destroyBtn = makeButton("DestroyBtn", "Destroy", 320, 170) -- Changeable

local function notify(titleText)
    game.StarterGui:SetCore("SendNotification", {
        Title = titleText,
        Text = "",
        Duration = 3
    })
end

checkKey.MouseButton1Click:Connect(function()
    if textbox.Text == "What's the key? Place here!" then -- Changeable
        notify("Valid Key!") -- Changeable
        gui:Destroy()
        loadstring(game:HttpGet("Link"))() -- Changeable
    else
        notify("Invalid Key!") -- Changeable
    end
end)

getKey.MouseButton1Click:Connect(function()
    notify("Copied!") -- Changeable
    setclipboard("Link Here!") -- Changeable
end)

destroyBtn.MouseButton1Click:Connect(function()
    gui:Destroy()
end)